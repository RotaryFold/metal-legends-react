import React from "react";
import "./hero.css";

const Hero = () => {
  return (
    <section className="hero-section" id="home">
      <div className="hero-content">
        <h2 className="hero-title">Discover the Icons of Metal</h2>
        <p className="hero-subtitle">Bands, gigs and legacy — curated for metalheads.</p>
        <a className="btn-primary" href="#bands">Discover Bands</a>
      </div>
    </section>
  );
};

export default Hero;






